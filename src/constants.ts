// ─── Shared layout and UI constants ───────────────────────────────────────────

// Graph node dimensions — shared between CatenoGraph (layout) and CatenoNode (rendering)
export const NODE_W = 200;
export const NODE_H = 72;
export const W_FOCUS = 230;
export const H_FOCUS = 108;

// Graph layout
export const COLUMN_WIDTH = 280; // world-px between causal depth columns
export const Y_STEP = 120; // vertical gap between same-column node centres

// App chrome heights — used for initial viewport calculation
export const HEADER_H = 60;
export const TIMELINE_H = 44;

// Detail panel
export const PANEL_BG = "#141414";
export const PANEL_WIDTH = 340;
